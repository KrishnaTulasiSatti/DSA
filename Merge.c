/*  Divide and Conquer Sorts --> Merge Sort,Quick Sorts
Pre-Requisites
Merge Sort:
1.Merging two sorted arrays
Define :
You will be given 2 arrays A and B of sizes m and n respectively
A is a sorted array,so is B.
Target : Create a new array which is also sorted
2.Merging two sorted portions of same array
Quick Sort:
1.Partition: Lomuto Partition,Hoare's Partition
*/
     //Merging two sorted arrays
 #include<stdio.h>
 int main(){
 	int m,n;
 	scanf("%d %d",&m,&n);
 	int i;
 	int a[m],b[n];
 	for(i = 0 ;i < m ; i++ ) scanf("%d",&a[i]);
 	for(i = 0 ; i < n ; i++) scanf("%d",&b[i]);
 	int result[m+n];
 	
 } 

